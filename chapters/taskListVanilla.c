#include stdio.h
#include taskListVanilla.h

Int main () {

//vars here

//read in the file name. Maybe just use a file with each if four parts of task list on its own line, or comma separate. 


//print statements
//print formatting always for a LaTeX template. Call the specific task list number dynamically. 
//when it looks nice, loop through task lists to compile all the LaTeX pages in one run. 

//output text into separate. .tex files
//include some error system or maybe counter that indicates if it was done correctly. 
}


